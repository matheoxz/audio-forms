# Development environment configuration for MongoDB
# (spring.profiles.active is set in application.properties)
spring.data.mongodb.uri=mongodb://localhost:27017/audio-forms

# Frontend URL for CORS during development
app.frontend.url=http://localhost:3000
